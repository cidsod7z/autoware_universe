name: check-build-depends

on:
  pull_request:
    paths:
      - build_depends*.repos

jobs:
  check-build-depends:
    uses: ./.github/workflows/build-and-test-reusable.yaml
    with:
      rosdistro: jazzy
      container: ghcr.io/xmfcx/autoware-new:universe-dependencies-cuda-jazzy
      cuda: true
      runner: "['self-hosted', 'Linux', 'X64']"
      pull-ccache: true
